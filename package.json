{
  "name": "snap-sis-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint -- --fix"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
